{
  "name": "vangoux",
  "version": "1.0.0",
  "description": " Design page an introductory site Vangoux.",
  "source": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel",
    "build": "rm -rf dist && rm -rf .parcel-cache && parcel build ./src/*.html --no-cache --no-source-maps --dist-dir dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^17.6.6",
    "@commitlint/config-conventional": "^17.6.6",
    "husky": "^8.0.3",
    "parcel": "^2.9.3",
    "posthtml": "^0.16.6",
    "posthtml-include": "^1.7.4"
  }
}
